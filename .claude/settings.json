{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Project-level Claude Code config. Tracked in git so every session inherits the same safety guards. Per-machine state lives in .claude/skills, .claude/state, etc. (gitignored).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/check-git-add-all.sh"
          }
        ]
      }
    ]
  }
}
